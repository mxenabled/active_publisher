require 'spec_helper'

describe ActivePublisher do
  it 'has a version number' do
    expect(ActivePublisher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
